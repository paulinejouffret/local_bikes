name: 'bikes_data'
version: '1.0.0'
config-version: 2

profile: 'bikes_data'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bikes_data:
    staging:
      +materialized: view
      sales:
        +schema: staging_sales
      production:
        +schema: staging_production
